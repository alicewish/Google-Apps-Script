function runThis() {
    var documentId = "1w68zfn_0rpEV7XAxZUo5qmdN9aiNppeyUXOeN86_bO0"
    var forDriveScope = DriveApp.getStorageUsed(); //needed to get Drive Scope requested
    var url = "https://docs.google.com/feeds/download/documents/export/Export?id=" + documentId + "&exportFormat=docx";
    var param = {
        method: "get",
        headers: {"Authorization": "Bearer " + ScriptApp.getOAuthToken()},
        muteHttpExceptions: true,
    };
    var html = UrlFetchApp.fetch(url, param).getContentText();
    var file = DriveApp.createFile("愤怒的小鸟节日特刊雷神译.gdoc", docx);
    return file.getUrl();
    var documentId = "13GBFM4SCtFgeJOcZUtB_IQUTCRmB07UokKq1tud9vnQ"
    var forDriveScope = DriveApp.getStorageUsed(); //needed to get Drive Scope requested
    var url = "https://docs.google.com/feeds/download/documents/export/Export?id=" + documentId + "&exportFormat=docx";
    var param = {
        method: "get",
        headers: {"Authorization": "Bearer " + ScriptApp.getOAuthToken()},
        muteHttpExceptions: true,
    };
    var html = UrlFetchApp.fetch(url, param).getContentText();
    var file = DriveApp.createFile("愤怒的小鸟迷你001雷神译.gdoc", docx);
    return file.getUrl();
    var documentId = "1Bd5j6VEbPWHpg7rvitw4ye8XvBD9EDJtaELjZ1T6QCY"
    var forDriveScope = DriveApp.getStorageUsed(); //needed to get Drive Scope requested
    var url = "https://docs.google.com/feeds/download/documents/export/Export?id=" + documentId + "&exportFormat=docx";
    var param = {
        method: "get",
        headers: {"Authorization": "Bearer " + ScriptApp.getOAuthToken()},
        muteHttpExceptions: true,
    };
    var html = UrlFetchApp.fetch(url, param).getContentText();
    var file = DriveApp.createFile("愤怒的小鸟迷你002雷神译.gdoc", docx);
    return file.getUrl();
    var documentId = "1lrO5mxltlM8f17kWeiPZFx1zY6HBrPWTCUafqHg5ko8"
    var forDriveScope = DriveApp.getStorageUsed(); //needed to get Drive Scope requested
    var url = "https://docs.google.com/feeds/download/documents/export/Export?id=" + documentId + "&exportFormat=docx";
    var param = {
        method: "get",
        headers: {"Authorization": "Bearer " + ScriptApp.getOAuthToken()},
        muteHttpExceptions: true,
    };
    var html = UrlFetchApp.fetch(url, param).getContentText();
    var file = DriveApp.createFile("愤怒的小鸟迷你003雷神译.gdoc", docx);
    return file.getUrl();
    var documentId = "1YHp6HYz8q_Fkt63N4JweGA8PJQz0YfXwprb8Hi9k6Zo"
    var forDriveScope = DriveApp.getStorageUsed(); //needed to get Drive Scope requested
    var url = "https://docs.google.com/feeds/download/documents/export/Export?id=" + documentId + "&exportFormat=docx";
    var param = {
        method: "get",
        headers: {"Authorization": "Bearer " + ScriptApp.getOAuthToken()},
        muteHttpExceptions: true,
    };
    var html = UrlFetchApp.fetch(url, param).getContentText();
    var file = DriveApp.createFile("愤怒的小鸟迷你004雷神译.gdoc", docx);
    return file.getUrl();
    var documentId = "1n9JEJ7a9vAv6iyEI0BqTIGFCsflRA-M8_A4mEoh0Trs"
    var forDriveScope = DriveApp.getStorageUsed(); //needed to get Drive Scope requested
    var url = "https://docs.google.com/feeds/download/documents/export/Export?id=" + documentId + "&exportFormat=docx";
    var param = {
        method: "get",
        headers: {"Authorization": "Bearer " + ScriptApp.getOAuthToken()},
        muteHttpExceptions: true,
    };
    var html = UrlFetchApp.fetch(url, param).getContentText();
    var file = DriveApp.createFile("愤怒的小鸟迷你005雷神译.gdoc", docx);
    return file.getUrl();
    var documentId = "1EhaGIe1wPqN2fTT6HR5-ED_fHqxR-X9Jx2Lg7OfhyiE"
    var forDriveScope = DriveApp.getStorageUsed(); //needed to get Drive Scope requested
    var url = "https://docs.google.com/feeds/download/documents/export/Export?id=" + documentId + "&exportFormat=docx";
    var param = {
        method: "get",
        headers: {"Authorization": "Bearer " + ScriptApp.getOAuthToken()},
        muteHttpExceptions: true,
    };
    var html = UrlFetchApp.fetch(url, param).getContentText();
    var file = DriveApp.createFile("愤怒的小鸟迷你006雷神译.gdoc", docx);
    return file.getUrl();
    var documentId = "10SmXkYkJMFUh6NS4MhV627akMvBWQgNdDVEeYMigjV0"
    var forDriveScope = DriveApp.getStorageUsed(); //needed to get Drive Scope requested
    var url = "https://docs.google.com/feeds/download/documents/export/Export?id=" + documentId + "&exportFormat=docx";
    var param = {
        method: "get",
        headers: {"Authorization": "Bearer " + ScriptApp.getOAuthToken()},
        muteHttpExceptions: true,
    };
    var html = UrlFetchApp.fetch(url, param).getContentText();
    var file = DriveApp.createFile("愤怒的小鸟迷你007雷神译.gdoc", docx);
    return file.getUrl();
    var documentId = "1z7v9XRAbwzidwZ59NB4C75B7UJ7J4JmO8rAzNfBGS8w"
    var forDriveScope = DriveApp.getStorageUsed(); //needed to get Drive Scope requested
    var url = "https://docs.google.com/feeds/download/documents/export/Export?id=" + documentId + "&exportFormat=docx";
    var param = {
        method: "get",
        headers: {"Authorization": "Bearer " + ScriptApp.getOAuthToken()},
        muteHttpExceptions: true,
    };
    var html = UrlFetchApp.fetch(url, param).getContentText();
    var file = DriveApp.createFile("愤怒的小鸟迷你008雷神译.gdoc", docx);
    return file.getUrl();
}